# DEF-DB-VOLUME-CEILING

> **STUB — R-IV.263(c), 2026-09-05.** Filed to retire a phantom, not to diagnose one.
> **Everything below is read from CITING CONTEXT ONLY. No investigation was performed**,
> no code was read for this stub, and nothing here is a measurement by this lane.
> Citations counted mechanically over `docs/` at HEAD `727d609`.

**Cited:** 4 times across 2 files.

## What the citations say

> **Supersedes:** DEF-DB-VOLUME-CEILING — this registration holds that defect's real content.

> "~1.3GB the database cannot see" recorded against DEF-DB-VOLUME-CEILING.

## Status: SUPERSEDED — not open, not lost

**Its content lives at** `docs/codex-briefs/2026-08-02-registration-def-pgss-textfile-growth.md`. The successor registration says so on its own
face, which is why this stub is a pointer rather than an investigation.

**The phantom here is a naming artifact.** The original name kept being cited after its
content moved, so a reader following the citation found nothing — not because the work
was lost, but because the pointer was never written down. **A superseded defect still
needs a file**, and it needs exactly one line: where its content went.

## Substance, for orientation only

The recorded figure is ~1.3 GB the database cannot see — growth outside what
`pg_database_size` and `pg_ls_waldir` report. The successor registration identifies the actual
consumer. **Read the successor; nothing here is a measurement.**
