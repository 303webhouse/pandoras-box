# DEF-MCP-LENS-TZ · P2, spine-authored (per citation)

> **STUB — R-IV.263(c), 2026-09-05.** Filed to retire a phantom, not to diagnose one.
> **Everything below is read from CITING CONTEXT ONLY. No investigation was performed**,
> no code was read for this stub, and nothing here is a measurement by this lane.
> Citations counted mechanically over `docs/` at HEAD `727d609`.

**Cited:** 4 times across 3 files — `docs/edge/RECOMMENDATIONS.md` and `docs/edge/TRUSTABLE-DATA-MAP.md`.

## What the citations say

> **REC-002** MCP-lens serialization — spine-authored brief DEF-MCP-LENS-TZ

> DEF-MCP-LENS-TZ (spine-authored, P2) — on acceptance (rows …

> Successor brief DEF-MCP-LENS-TZ (spine-authored, P2) delivered;

## Status: AUTHORED ELSEWHERE, no artifact under this name

The citations describe it as a **spine-authored brief that was delivered**. So the work
exists; what is missing is a file findable under the cited name. **This is the phantom
class where the artifact exists under a different identity** — the sweep's
"dedicated artifact elsewhere" bucket held 18 such cases, and this one was not matched
to any of them.

## Substance, for orientation only

The subject is MCP-lens timezone serialisation. **This lane already carries an
operational rule from the same phenomenon**: the Postgres MCP renders naive timestamps
shifted by the local offset, so clock questions are adjudicated inside the database and
never from an MCP read. That rule cost a wrong P1 fix before it was written down.

**Whether this defect is that phenomenon is not established here** — the names and the
subject match, the identity does not follow from that, and no comparison was made.

## What a real artifact would need

The delivered brief located and either filed under this name or cross-referenced from
it. That is a search this stub did not perform.
