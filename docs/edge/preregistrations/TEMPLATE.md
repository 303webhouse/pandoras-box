# EDGE Pre-Registration — PR-___ : [short name]
Status: REGISTERED | RESULT-FILED   (sections 1–6 immutable once REGISTERED)
Filed: YYYY-MM-DD HH:MM MT — before any result data was viewed
Mode: CONFIRM | EXPLORE  (EXPLORE findings spawn a NEW PR on unseen data;
they never conclude)

## 1 · Hypothesis
One falsifiable sentence.

## 2 · Population & direction
Strategy / signal source:
Direction: LONG | SHORT  (never pooled — one PR per direction, or explicit
per-direction sub-tests)
Instrument class / bucket (B1/B2/B3):

## 3 · Sample window
Window (id-range keying preferred while P1 timestamp shift is unresolved;
state keying used):
Exclusions (deaf windows per Trustable Data Map, drill artifact ids, other):
Expected n per direction:
n-gate (below this n, verdict is "insufficient n" regardless of result):

## 4 · Metric & threshold  (stated before looking)
Primary metric, after costs — cost model stated (fees / spread / slippage):
Success threshold:
Stability: must hold in >=2 non-overlapping sub-windows
Robustness: re-run excluding P6-poisoned factors (if factor-conditioned)

## 5 · Multiple-comparisons disclosure
Tests run in this family to date, including this one:

## 6 · Data integrity
Tables touched + Trustable Data Map status (VALUE/DEGRADED/UNAVAILABLE):

## 7 · Result  (append-only; never edit sections 1–6)
Executing spec: QS-__    Run date:
Raw result:
Verdict: VALUE | DEGRADED | UNAVAILABLE | DISCARDED
Notes:
