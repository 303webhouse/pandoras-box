# EDGE Recommendations Ledger · v1.1 · 2026-08-21

Statuses vintage-stamped as-of 2026-08-21.

**REC-001** enforced read-only Postgres role — PRE-ACCEPTED (spine 08-01); routing
unscheduled as of this filing.

**REC-002** MCP-lens serialization — spine-authored brief `DEF-MCP-LENS-TZ`
delivered (P2, 08-17); acceptance = rows 27774 / 27777 identical via MCP and
`to_char`; on PASS, Map §0-R1 retires for converted surfaces.

**REC-003** ANALYZE / autovacuum hygiene — REGISTERED, bundled with
STABLE-DAILY-BARS-STATS.

**REC-004** `strategy_health` expectancy remediation — ROUTED with F-EDGE-001;
EXPANDED (spine 08-17): renders UNGRADED / DEAD for zero-lifetime-outcome
strategies; daily corpse scores stop.

**REC-005** `unified_positions.quantity` → NUMERIC — RULED: ATLAS-lens schema
brief; census-with-count · in-migration backfill of three truncated rows ·
sequenced after queue head.

**REC-006** trade↔signal linkage — APPROVED (spine 08-17) as the infrastructure
verdict; joint read-only Phase 0 serves 006 + 005, then two ATLAS-lens briefs.
Design laws: entry-time `signal_id` or explicit `NO_SIGNAL` / `DISCRETIONARY`;
backfill only where derivable, stated confidence, underivable stays labeled.

**REC-007** ORPHAN-TRAP correlation instrument — shape APPROVED (R-IV.21-era);
micro-brief drafted per delegation (b8034c1 positive control + cannot-abort proof
in acceptance); build grading not yet relayed to EDGE as of this filing.
Status note: no live capture available (bucket-3 empty, LETH-1) — the reconciler
remains the instrument for ORPH-SPORADIC mode.

**REC-008** swallow-hardening — APPROVED as drafted (R-IV.32(d)).
Census (refuter): 139 broad handlers · 0 re-raise · 109 swallows · 30 fully
silent. Motivating specimen: NaN-POISON — per-row INSERT rejection swallowed to
✅, 459 rows lost. Kinship: REC-007's loudness generalized; the fake-healthy
taxonomy's structural fix. Scope / sequencing: spine's.
