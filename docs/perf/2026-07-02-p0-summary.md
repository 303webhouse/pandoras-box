# P0 "Subtraction & Speed" — Lighthouse before/after

Harness: Chrome headless, Lighthouse 12, `--preset=desktop`, frontend served from a
local static server (`frontend/` on :8792). API calls 404 locally, so both runs share
identical (backend-less) conditions — deltas are attributable to the frontend changes.
Reports: `2026-07-02-p0-before.report.{html,json}` / `-after.report.{html,json}`.

| Metric | Before | After | Delta |
|---|---:|---:|---:|
| Performance score | 93 | 95 | +2 |
| First Contentful Paint | 855 ms | 843 ms | −1.4% |
| Largest Contentful Paint | 1582 ms | 1424 ms | −10.0% |
| Time to Interactive | 1582 ms | 1424 ms | −10.0% |
| Total Blocking Time | 0 ms | 0 ms | — |
| Speed Index | 901 ms | 843 ms | −6.4% |
| JS bootup time | 59 ms | 1 ms | −98% |
| DOM nodes | 1387 | 1258 | −9.3% |
| **Initial same-origin JS** | **708.9 KB** | **563.7 KB** | **−20.5%** |

Initial JS drop = `laboratory.js` (97 KB) + `cockpit.js` (27 KB) lazy-loaded on first
Abacus open, plus `app.js` shrinkage from the Task 1 kills. `analytics.js` (112 KB,
unwired) deleted outright.

## Targets (Task 4g)
- Time-to-interactive < 2.5 s local → **1.42 s** ✅
- JS parsed on initial load down ≥ 15% → **−20.5%** ✅
- Idle interval count < 10 → **3 active when backgrounded** (visibility-hidden), and
  **1 excluding the shared 1 s ticker + 5 s stall watcher** ✅ (28 active while the
  tab is visible/foreground, all now `managedInterval`-gated so they pause on hide).

Idle-interval count measured by patching `setInterval`/`clearInterval` via a CDP
preamble and toggling `document.visibilityState`.
