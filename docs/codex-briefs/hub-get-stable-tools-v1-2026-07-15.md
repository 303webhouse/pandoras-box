# BRIEF — `hub_get_stable_*` v1 → Stable Engine board on MCP ("Brief 3")

**Recon anchor:** `origin/main` @ `42145e6`.
**Titans Pass 1:** ATLAS (backend/scalability) + AEGIS (security) ran as independent single-pass reviews (not the full 4-agent double-pass — one round each, not two). HELIOS abstain (backend-only, no UI surface). ATHENA synthesis performed directly by the orchestrating session (not a separate blind pass — PM/synthesizer role reads both inputs by design) → PROCEED, scope approved by Nick 2026-07-15. **4→6 tool-count expansion is ATLAS's architecture proposal** (theme_members + board_state as their own registrations, both justified on differing-freshness-contract / mislabeled-provenance grounds), ATHENA-endorsed in synthesis, Nick-approved — not a builder's call; CC has not yet touched this brief.
**Scope:** 6 new MCP tools (`REGISTERED_TOOL_NAMES` 15 → 21). `index-strip` and `sector-divergence` explicitly OUT (no demonstrated committee value; sector-divergence's raw shape is a dashboard sparkline payload, not reasoning-sized — needs an aggregate reshape before it's committee-viable, not in scope here).

## Purpose
Let the Olympus committee (TORO/URSA/THALES/PIVOT, Claude.ai-side, MCP-connected) read the Stable Engine regime/theme/movers/rates-fx board directly, instead of dashboard-only. Post-flip ticket named in `docs/codex-briefs/2026-07-03-dashboard-rebuild-v2.md`'s Olympus-impact note. Stable Engine is yfinance-only (zero UW calls) — **no `uw_governor.py` QUOTAS entry needed**, unlike `chart_indicators`.

## Tool list
| Tool | Source | Notes |
|---|---|---|
| `hub_get_stable_regime` | `/api/stable/regime` | regime_label + breadth + dominant/emerging/fading |
| `hub_get_stable_themes` | `/api/stable/themes` | ranked table, curated fields only (see Task 4) |
| `hub_get_stable_theme_members` | `/api/stable/theme/{t}/members` | own tool, not a param on `themes` — different freshness contract (RTH live overlay), don't blur two response shapes into one tool description |
| `hub_get_stable_movers` | `/api/stable/movers` | gainers/losers |
| `hub_get_stable_rates_fx` | `/api/stable/rates` + `/api/stable/fx` | bundled, 2 nested sub-blocks |
| `hub_get_board_state` | `/api/board/tide` + `/api/board/kill-switch` | bundled; NOT named `stable_*` — different router/provenance (UW-cache tide + circuit-breaker process state, not Stable Engine) |

## Tasks (summary — see CC session for full text)
- **Task 0 (prerequisite, do first):** fix `.github/workflows/mcp-lint.yml`'s path glob — watches `backend/mcp/**`, real module is `backend/hub_mcp/**` (renamed same commit the workflow was added, `52e1038`; glob has never matched). The import-safety lint has **never run in CI**. Fix this before anything else so the rest of this brief's controls are actually enforced, not aspirational.
- **Task 1 (extraction, no behavior change):** new `backend/services/read_only/stable.py` (`get_themes/get_regime/get_theme_members/get_movers/get_rates/get_fx`) + `backend/services/read_only/board.py` (`get_tide/get_kill_switch`). Mirror the existing `/levels` → `market_profile.py` precedent in `board_state.py`, not a copy-paste of the SQL. `api/stable.py` + `api/board_state.py` route bodies shrink to thin callers. Wrap each function's DB call in try/except → `None` on failure (currently absent in the route functions — exception text would otherwise propagate raw through FastMCP to the client once a hub_mcp tool calls in). Verify via before/after JSON diff on all 8 touched REST endpoints — dashboard must render byte-identical.
- **Task 2:** `backend/hub_mcp/stable_envelope.py` — one shared `map_stable_status(as_of, anchor, degraded, flatline, feed) -> (status, staleness_seconds)` helper, used by all 6 tool files. Precedence: no snapshot → `unavailable` + empty data shell; `flatline` (or computed `age > SLO_SECONDS[feed]` for feeds with no native flatline flag — rates/fx/index-strip skip age checks upstream, don't inherit that gap) → `unavailable` **but keep last-known `data`**, echo `flatline=true` inside it, `error="feed_flatline:<feed>"` (deliberate break from the null-on-unavailable precedent elsewhere — flatline means the pipe died, not that nothing was ever fetched; hiding real historical data helps nobody); `degraded` (REST-native) → `degraded` 1:1; computed `age > 0.5×SLO_SECONDS[feed]` → `stale`; else `ok`. `anchor="provisional"` alone is NOT degradation (normal steady-state for movers/rates/fx). `as_of`/`anchor`/`degraded`/`flatline` always surfaced inside `data`, never dropped. Multi-block tools (`rates_fx`, `board_state`): top-level `status` = worst of sub-statuses; kill-switch failure must dominate tide, never average out.
- **Task 3:** `hub_mcp/tools/stable_regime.py`. No params. Cache the `breadth` block (window-function query over ~276K rows, the one genuinely expensive query in the set) keyed by `stable_metrics.metrics_date` in Redis (not a wall-clock TTL — breadth is invariant until the next nightly/provisional job writes a new row), reusing `get_redis_client()`.
- **Task 4:** `hub_mcp/tools/stable_themes.py`. No params. **Two non-negotiable requirements:** (a) curated field set only — `{theme, rank, score, score_1d_delta, status, n_names}`, drop the 13 dashboard-only sub-metrics (extension_raw, pct_above_*ma ×4, pct_new_high ×2, avg_ret_5d/20d, avg_atr_ext_50ma, avg_rs_qqq_20d) that inflate every committee read for no reasoning value; (b) `data_quality_warnings` field — hardcoded `["Robotics score is known-corrupted (delisted-ticker LAZR bug), do not treat as genuine momentum"]` whenever Robotics is present in the response, until the separate LAZR-fix brief ships. This is a **hard requirement**, not deferrable — this brief is what first exposes the corrupted number to a decision-making agent instead of a human with context. **This is a tool-output guard ONLY — it does not fix the upstream score computation.** The 3-layer root-cause fix (evict LAZR, sanity-bounded pct_change, quarantine-on-resume) is a separate, not-yet-written brief (orphaned P3 backlog item, scoped 7/10, never built) — Brief 3 shipping does NOT close that item; it stays open in the ledger. Mirror the `warnings`-list + `degraded` pattern already in `hub_get_sector_strength` (`sector_strength.py:133-137`).
- **Task 5:** `hub_mcp/tools/stable_theme_members.py` → `hub_get_stable_theme_members(theme, top=5, bottom=5)`. **Fix the underlying cost bug, not just add a cache**: `get_theme_members` currently live-fetches prices for the theme's *entire* roster before slicing to top/bottom (slice happens after fetch) — reorder so it sorts on the last-known `ret_1d`, takes the top+bottom slice first, *then* live-fetches only those ≤~100 tickers. On top of that, memoize the live-overlay result in Redis keyed `(theme, minute-bucket)`, 60s — caps external-call rate to once/minute/theme regardless of committee fan-out (rate limiter is global-per-token across all tools, not per-tool, so nothing else bounds this). Tool description must state the polling-cadence expectation explicitly (e.g. "live overlay only during RTH; don't call more than once/minute per theme — the base `themes` snapshot is cheaper and sufficient outside that cadence"), since nothing enforces cadence at the protocol layer.
- **Task 6:** `hub_mcp/tools/stable_movers.py`. No params.
- **Task 7:** `hub_mcp/tools/stable_rates_fx.py`. Bundles rates + fx, nested sub-blocks, worst-of-two status per Task 2.
- **Task 8:** `hub_mcp/tools/board_state.py` → `hub_get_board_state()`. Bundles tide + kill-switch, worst-of-two status, kill-switch dominates.
- **Task 9 (wiring):** `decorators.py` `REGISTERED_TOOL_NAMES` (+6), `tools/__init__.py` (+6 imports). No governor QUOTAS change.
- **Task 10 (close the import-isolation gap):** add `api.stable` + `api.board_state` to `scripts/lint_mcp_imports.py`'s `FORBIDDEN_MODULES` — Task 1's extraction makes direct import unnecessary; this makes it unenforceable-to-skip. Depends on Task 0 actually running the lint.
- **Task 11:** wire the `stable:regime:breadth:{metrics_date}` Redis cache into `services/read_only/stable.py::get_regime()` per Task 3.
- **Task 12 (skills):** `skills/thales/SKILL.md` — add all 6 tool names to Context-A (primary macro/regime consumer). `skills/pivot/SKILL.md` — reference `hub_get_board_state` explicitly as a pre-synthesis gate check (kill-switch active → cap/floor recommendation, matching existing bias_cap/bias_floor semantics in `board_state.py`'s kill-switch payload). Re-upload both full folders, verify in a fresh chat.
- **Task 13 (recommended, not blocking — fold in only if scope allows):** the audit log never actually records tool-call arguments — `RateLimitMiddleware` extracts only `tool_name` from the JSON-RPC body, `AuditMiddleware` calls `log_call(parameters=None, ...)` unconditionally (`router.py:138,215`), so `hash_parameters()` always hashes `{}`. Pre-existing bug across all 14 current tools, not Brief-3-specific, but Brief 3 is what surfaces why it matters (can't audit "which theme was queried" on an incident). If fixed: thread `arguments` from the parsed JSON-RPC body into `log_call` alongside the existing `tool_name` extraction. For these 6 tools specifically, log params **in the clear, not hashed** — `theme`/`window` are small fixed public enums, not position-revealing like a ticker on `hub_get_quote`; hashing a keyspace that small is trivially reversible and just destroys log usefulness for zero confidentiality gain. Don't change the hashing default for ticker/position-adjacent tools.

## CC Phase-0 deltas (verify before Task 3/11 lands — don't guess)
1. **Railway replica count for `backend/main.py`** — unconfirmed. If single-worker, in-process dict memoization is simpler and preferable to Redis for the regime breadth cache; if multi-worker, Redis is required for cache coherence across replicas. Check `railway.json`/service config or `railway status` before committing to the Redis-keyed design in Task 3.
2. Confirm `get_rates()`/`get_fx()`/`get_index_strip()` really have no age-based `degraded` check upstream (only `get_movers()` does) — Task 2's uniform staleness band assumes this; verify against current `stable.py` before wiring `SLO_SECONDS` fallback logic, in case it's been fixed since this brief was written.
3. `schema_version` — confirm current value in `hub_mcp/__init__.py` (found as `"v2.0"` during research, contradicting `envelope.py`'s docstring which still says `"v1.0"` — stale comment, not a live discrepancy, but verify before assuming no bump is needed).

## Gates
No raw SQL in `hub_mcp/*` — all queries via `services/read_only/*`. `make_response()` only, no direct dict construction in tool files. `flatline`/`degraded`/`anchor`/`as_of` never silently dropped — always echoed inside `data`. Robotics/LAZR guard present on `stable_themes` and `stable_regime`, clearly commented as temporary/removable once the LAZR-fix brief ships. `REGISTERED_TOOL_NAMES` goes 15 → exactly 21, no silent extras. Zero net-new UW governor quota entries. `mcp-lint.yml` path glob fixed and passing with `api.stable`/`api.board_state` now blacklisted (Task 0 + Task 10 both land, in that order). `theme_members`'s live-fetch is slice-then-fetch, never full-roster-then-slice. No push to `main` 07:30–14:00 MT on trading days without Nick's explicit same-session override (standing rule).

## Done
All 6 tools boot; each demonstrated in ok/degraded/stale/unavailable states against live data (at least one forced case per tool — e.g. bogus theme name for `theme_members`, simulated flatline for regime). Regime breadth cache verified to skip the window-function query on a 2nd call within the same `metrics_date`. `hub_get_stable_themes`/`hub_get_stable_regime` confirmed carrying the Robotics warning while the LAZR bug is live (upstream fix stays a separate open P3 item — not closed by this brief). Dashboard renders identically post-extraction (Task 1 before/after diff, zero behavior change from the refactor alone). `lint_mcp_imports.py` passes in CI (not just locally) with the two new blacklist entries. **Measured per-day external-call budget for `hub_get_stable_theme_members`, verified post-deploy** — these are yfinance calls (not UW, so untracked by `uw_daily_burn`/the governor); log or count actual daily volume for the first 2-3 days post-ship and confirm the slice-then-fetch reorder + 60s per-theme cache are actually holding it near the once/minute/theme design target, not just assumed from the code. THALES + PIVOT Olympus retest: THALES pass on a sector-regime-shift trigger, PIVOT pass confirming kill-switch gate checked pre-synthesis. Toggle the Pandora connector after ship (manifest refresh for the 6 new tools). Closure note in `docs/strategy-reviews/` or `docs/workstreams.md`.
