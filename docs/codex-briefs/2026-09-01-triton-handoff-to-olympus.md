# TRITON — OWNERSHIP HANDOFF TO OLYMPUS LANE · 2026-09-01
From: Fable (spine, context 1, retiring). Authority: principal directive 2026-09-01.
The Olympus lane now OWNS the Triton track end-to-end: audit consumption, explore,
re-scope proposal, and the full-strategy shadow-tracking commission (§5). The spine
(successor context) retains ratification + cross-lane sequencing only.

## 1 · Founding scope — read the artifact, never summaries
docs/codex-briefs/2026-06-15-triton-build-handoff.md is the founding design. Elements:
Mode A absorption (whale_hunter_v2.pine, DORMANT) · Mode B aggression (Trojan Horse /
FOOTPRINT webhook, LIVE) · HG-Raschke as Mode-B regime FILTER only (strategy itself is
KILL-CONFIRMED, PR-105) · darkpool_enrichment (UW 4h directional prints — SHADOW ONLY,
confluence_bonus=0, annotates never scores) · Market Tide (never built) · PYTHIA
structure gate (POC/VAH/VAL/IB) + Artemis VWAP±2σ bands harvested in · Tempo horizon
tag (SCALP/SWING/POSITION from mode+ADX+target÷ATR, GEX-adjusted) · timing gates
(Tue–Thu, ≥15min post-open, lunch filter) · ~16-name Tier-1 inside ~80 universe, VIX
and leveraged ETFs excluded · UW-dark fail-safe (URSA P0) · data-fidelity ladder:
dark-pool prints > footprint > tape > 1-min POC proxy.

SCOPE BOUNDARY, binding on all verdict reading: the shadow study tests ONLY the sweep
premise (follow vs fade, $250K/$750K/$2M tiers). A KILL kills sweep-following and
re-scopes L2; it does NOT touch the untested pillars above. A survival validates the
crudest ladder rung only. Neither outcome speaks to dark-pool/absorption/tide/timing.

## 2 · Evidence machine — census of record 2026-08-31 21:29Z
7,014 total · 6,045 graded (fired ≤08-14) · 969 pending. POLLER ALIVE (72 rows 08-31).
GRADER DARK since 08-27 — DEF-TRITON-GRADER-DARK, P2, queued at CC-BUILD behind SPEC-01
(Phase-0 why-stopped → restart → backfill 843 → register grader in signals_freshness).
DEF-TRITON-RETENTION-DARK, P3, fix BLOCKED by law: "no deletion policy may run while
the reason rows are undeleted is itself unexplained" — generalized: no deletion may
outrun the liveness of any consumer holding an unexercised claim. The 46 pre-08-01
ungraded rows are the physical evidence of the index-symbol class; a 30-day sweep
would erase the evidence and keep the defect.
DEF-TRITON-DEAD-FIELDS: is_sweep 100% TRUE, chg_pct_day 100% NULL (n=7,014) — any
conditioning on either returns something that looks like an answer. DEF-TRITON-GRADER-
NO-SKIP-REASON: grader records no reason for skips (design note: every skip records
its reason). DEF-TRITON-INDEX-UNGRADEABLE: SPX/SPXW/RUT/RUTW/VIX have no price series,
0 of 72 ever graded — grader fix must classify them UNGRADEABLE-NO-SERIES.

## 3 · Holdout — REGISTERED, identity-pinned, frozen
docs/edge/preregistrations/triton-holdout-registration-2026-09-01.md (EDGE-authored,
registration of record after a triple path-collision; R-IV.140 edits pending — see §6).
PIN as captured, NEVER re-derived: registered_at 2026-09-01 03:28:12Z · MAX_ID 377783 ·
n=843 · ids 305533…377783 · fired 08-17 13:30Z → 08-31 19:57Z · graded 0.
Grading does NOT release the set — no-peek binds on OUTCOMES regardless of graded_at,
until the explore artifact files. Any validation on it = its own CONFIRM registration,
criterion fixed before any split; conditioning fields named + verified non-degenerate.
Metadata (counts, dates, ticker) stays queryable. k = index-symbol rows inside the 843:
capture AUTHORIZED (R-IV.140), one aggregate read — result may still be pending; chase.
Effective validation n = 843 − k. Single-regime caveat: a failure here cannot separate
"no edge" from "not this regime" — state both readings or neither.

## 4 · Audit — executes 09-02, hard stop 09-03, artifact EOD 09-04 or ESCALATES
Brief: docs/codex-briefs/2026-07-21-triton-shadow-audit-brief.md — EXECUTE AS FILED
(R-IV.131; no re-review, criteria unchanged, dataset growth strengthens). Riders:
· Population by DATE never state: fired_at < 2026-08-17 AND graded_at IS NOT NULL
  (equivalently id < 305533). Post-repair, graded_at-alone sweeps the holdout in.
· Three handles + TRIPWIRE (R-IV.138): date · id<305533 · completeness identity
  audit_n + residue_pending + 843 = 7,014 on id ≤ 377783 (invariant under grading).
  audit_n ∈ [6,045, 6,099] and residue ≥ 72 — outside → HALT, report, no computation.
· Dead-field pre-flight: enumerate every conditioning/split field (is_liquid20,
  is_megacap_ai, +any) against the dead-field census; dead → NOT APPLICABLE.
· FACE LINES verbatim (R-IV.134 + R-IV.137): "Graded population ends 2026-08-14. Any
  recency- or regime-conditioned result is UNAVAILABLE, not 'no effect.'" · "The graded
  population excludes cash-settled index symbols entirely (SPX, SPXW, RUT, RUTW, VIX —
  72 interior rows, no price series, never graded). Findings describe single-name and
  ETF flow and do not extend to index flow." · 6,045 rows are EXPLORE; any edge claim
  requires its own CONFIRM registration, criterion fixed before any split.
Verdict semantics, pre-registered: H1 follow / H2 fade. Both dead → premise KILLED,
L2 re-scoped. Either alive → Triton v0 unblocked, still behind the L1a gates (6/29).
§6 DISCIPLINE BINDING: the principal's enthusiasm funds the investigation's priority,
never the verdict. KILL is a valid pre-registered outcome; no softening, re-slicing,
or post-hoc extension. Explore brief runs ONLY after the audit artifact files:
docs/codex-briefs/2026-07-21-triton-explore-brief.md (no-peek rules intact).

## 5 · NEW COMMISSION — full-strategy shadow tracking (principal-directed 09-01)
The principal expected the FULL element set to have been accumulating since June; only
the sweep shadow was. The June build was halted on URSA's challenge (premise untested);
collectors for the other pillars were never commissioned. Close the gap:
Step 1 — consume CC-QUERY's element-tracking census (R-IV.144, issued 09-01): what
  exists for dark-pool annotations, FOOTPRINT signal rows, whale rows, market tide.
Step 2 — post-verdict, propose the shadow-collector set for the untested pillars
  (dark-pool directional prints · Market Tide · footprint/absorption per the
  data-fidelity ladder), each with pre-registered expected rates and watermarks per
  the observation-instrument laws (OBS-0 liveness sentinel REQUIRED · bands from
  observed daily distributions, n-gated, never period averages · guards anchored to
  the window they guard · intermittent sources carry an as-of).
Step 3 — Titans review → principal greenlight → CC-BUILD executes. Shadow-first, zero
  scoring writes, UW-budget-aware (the 07-17 watchdog-shed precedent is real).

## 6 · Open items riding into this handoff
· R-IV.140 registration edits may not have reached CC-BUILD (BUILD reported "§4/chain
  unruled" AFTER the ruling issued): §4 straddle-clause replacement (EDGE ITEM-1 text) ·
  §2 three-handles clause · §6 not-a-kill-on-its-own-evidence clause · chain-of-record
  block · census re-file to docs/edge/results/2026-09-01-triton-holdout-pin-and-
  residue-census.md. Verify landed; re-issue through spine if not.
· k-capture result (R-IV.140) — chase from CC-QUERY, append to registration §1/§5.
· Grader-dark fix sequencing: BUILD queue, behind SPEC-01; retention fix stays BLOCKED.

## 7 · Rules that bind this lane
Artifacts assert, recall proposes (8 sub-forms + one-line check, docs/conventions/) ·
census-first · single-run, annotation by patch, erratum never silent · NOT DETERMINABLE
over a plausible number · file-route for payloads, relay carries filename+fingerprints ·
falsified-findings ledger board-wide · findings-only reporting (R-IV.99 triage).
REPORT TO SPINE at: audit artifact filed · explore filed · re-scope proposal ready.
Spine ratifies verdict consumption, the re-scope, any build order, any deletion.
