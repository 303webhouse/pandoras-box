# Brief S-2 — Regime & Session Layer — Completion Report

**Date:** 2026-07-15 | **Brief:** `docs/codex-briefs/2026-07-15-stater-swap-s2-regime-session-brief.md`
**Commits:** Phase 0 `413e665` | Task 0.0/1.0 docs `5f08146` | Phase 1-5 build `919411c`
**Amendments applied:** Fable's A (strategy_classes exact casing), B (event_windows NY-local), C (dismiss-reason via notes-append), D (hourly slot, default-enabled, reuse Wilder ADX), E (Done-13 baseline, unauthenticated endpoints) — all incorporated, none deviated from.

## Files touched

**New:** `migrations/025_crypto_regime_session.sql`, `backend/config/crypto_gate_config_seed.py`, `backend/config/crypto_gate_loader.py`, `backend/jobs/crypto_regime.py`, `backend/utils/crypto_sessions.py`, `backend/bias_filters/crypto_gates.py`, `backend/tests/test_crypto_sessions.py`, `backend/tests/test_crypto_gates.py`.
**Modified:** `backend/database/postgres_client.py` (mirrored DDL + idempotent seed), `backend/jobs/crypto_bars.py` (additive `fetch_crypto_ohlc()`, existing callers unchanged), `backend/scheduler/bias_scheduler.py` (hourly job registration + status entry), `backend/signals/pipeline.py` (new gate-evaluation hook, no existing branch to extend), `backend/api/crypto_market.py` (two new endpoints).

## Done Definition — status + evidence

1. **Phase-0 findings committed** — `docs/strategy-reviews/stater-swap-redesign/s2-phase0-findings.md`, commit `413e665`. **MET.**
2. **D-1 stamp live** — S-1 closure note's Sign-off section now reads: *"Both acknowledgments are now on record and the gate... is cleared: Nick — SIGNED, 2026-07-15 22:28Z (16:28 MDT)... Fable — COUNTERSIGNED, 2026-07-15... R-1 work (Brief S-2...) is cleared to begin."* **MET.**
3. **D-2 outcome recorded** — both artifacts found locally at their exact cited paths, committed as-is: `docs/codex-briefs/2026-07-13-stater-swap-s1-foundation-brief.md`, `docs/strategy-reviews/stater-swap-redesign/2026-07-13-titans-review-stater-swap-v2.md` (commit `413e665`). **MET.**
4. **D-3 both anchor edits live** — `symbol-capability-matrix.md`'s ZEC row + Finding 3 caveat, commit `413e665`. **MET.**
5. **Migration 025 applied, all three tables exist** — confirmed live via `information_schema.tables` on the deployed DB: `crypto_gate_config`, `crypto_gate_shadow`, `crypto_regime_log` all present. `-- DOWN` block in the migration file. **MET.**
6. **Config seeded, `gating_enabled=false` confirmed on the running service's DB** — queried live: exactly one seed row initially (`id=1`, `created_by='SEED_S2'`, `gating_enabled=false`), `event_windows` carries the real five-window Phase-0 inventory (exact NY-local boundaries, not the brief's placeholder string). **MET.**
7. **Regime rows real** — manually triggered one live evaluation cycle (`run_crypto_regime_job()`) on the deployed service: **6/6 symbols written**, all with distinct, non-zero price/dma50/adx14/slope values (anti-fake-healthy check passes — not identical or zeroed), correct per-symbol `bars_source` (UW_OHLC for BTC/ETH/SOL, BINANCE_SPOT for ZEC, OKX for HYPE/FARTCOIN), `degraded=false`/`degrade_reason=null` for all six (bar counts 226-500, well above both thresholds), BTC-USD `is_master=true` and every other symbol `is_master=false`. **MET.**
8. **Hot-reload proven** — INSERT'd a benign config change directly via SQL (`adx_trend_min` 20→21, `created_by='S2_HOTRELOAD_PROOF'`, new `config_version=2`) on the live DB; the very next regime evaluation (same process, no redeploy) picked it up immediately — loader returned the new version, and the freshly-written `crypto_regime_log` row for BTC-USD stamped `config_version=2`. Zero redeploys. **MET.**
9. **Session engine proven** — 12/12 DST unit tests green (`tests/test_crypto_sessions.py`): partition edges, Friday-only gating, real 2026 US DST transitions (spring 2026-03-08, fall 2026-11-01) proving the UTC window shifts while the NY-local definition is constant, Denver dual-label correctness on both MDT (-06:00) and MST (-07:00) dates, and a generic non-US IANA zone proving the mechanism isn't NY-hardcoded. Live `GET /api/crypto/clock` captured: dual labels present and correct, `partition="ASIA"` matched the request-time UTC hour, `next_transitions` sorted and spot-checked (`london_open` at `08:00Z` / `02:00-06:00` Denver — correct 6h offset for MDT). **MET.**
10. **Shadow gate end-to-end + failure-injection** — fired `S2_PHASE4_GATE_SHADOW_TEST_BTC_20260715` through the real deployed `process_signal_unified()`: `crypto_gate_shadow` row landed with sane fields (`regime_master=regime_symbol="CHOP"` since BTC is its own master, `session_partition="ASIA"`, `verdict="WOULD_PASS"`, `reasons=["UNCLASSIFIED_STRATEGY"]`, `config_version=2` — correctly picked up the hot-reloaded version from item 8). Failure-injection proof (`tests/test_crypto_gates.py::test_evaluator_exception_never_breaks_the_real_signal_write`): `evaluate_gates()` forced to raise `RuntimeError` — `process_signal_unified()` still returned normally and `log_signal` was still awaited (the real write survives). **MET.**
11. **Zero-live-impact assertion** — `gating_enabled=false` confirmed in both deployed config rows (`id=1` and `id=2`); `SELECT COUNT(*) FROM signals WHERE notes LIKE '%REGIME_GATE%'` = **0** across the entire table; the only `DISMISSED` row in the 2-hour window around this deploy was a pre-existing, unrelated equity conflict-dismissal (Artemis vs. Holy_Grail on CBRE) — confirmed via direct inspection, nothing to do with S-2. **MET.**
12. **Deployment verification, all 4 steps** — (1) Railway deploy status SUCCESS; (2) confirmed via the empirical checks below rather than a bare SHA compare — direct evidence the new code is live; (3) empirical side effects observed: all three new tables exist, config seeded correctly, live regime evaluation produced correct real data, hot-reload took effect with zero redeploys, `/api/crypto/clock` returns correct live data, one real signal produced a correct shadow-gate row — `/health=OK` was never treated as sufficient proof on its own; (4) logs checked, no errors or exception spam from the new code paths. **MET.**
13. **Known-red baseline unchanged** — Phase 0 recorded 18 FAILED tests (deviating from the brief's assumed 8, root-caused in the findings doc as two pre-existing local-sandbox environment issues, not application bugs). Post-Phase-1-5 build: **still exactly the same 18**, byte-for-byte identical test names, confirmed via direct diff. 297 passed (up from 273 — the +24 new tests this brief added). **MET.**
14. **Completion report + ACK** — this document. **MET.**

## Deviations, all disclosed inline above and none blocking

- Done item 6's original wording says "`signal_outcomes`" for the grading table in a different context (S-1, not reused here) — not applicable to S-2; noted for completeness only.
- No scheduler-registration-specific live proof beyond the manually-triggered function call, since (a) `run_crypto_regime_job_scheduled()` is a thin wrapper calling the exact same `run_crypto_regime_job()` function I invoked directly, and (b) the deploy would have failed to boot at all had the `scheduler.add_job(...)` call had a naming/import error (it's evaluated synchronously inside `start_scheduler()`, which the successful deploy already proves ran clean).
- `/api/crypto/state/{symbol}`'s existing `session`/`regime` placeholder fields (marked `_NOT_YET_BUILT_R1`) were **not** wired to the new real data — out of §7's explicit scope (only two new endpoints were asked for). Flagging as an easy, obvious follow-up now that the underlying data exists, not done here to avoid scope creep beyond the brief's own asks.

## Standing items for whoever picks up R-2+

- Bypass-retirement tracker (S-1 standing instruction) — re-check `scripts/crypto_dual_write_diff_report.py` before touching `bias_scheduler.py` again; still 3 test-only rows as of this brief, untouched by S-2.
- §10's shadow validation bar (≥14 days, n≥100 real gate-shadow rows, anti-bloat subtractive test, Nick greenlight) is explicitly **not** an S-2 exit criterion — S-2 closes with `gating_enabled=false` and the shadow dataset now accruing for real going forward.
