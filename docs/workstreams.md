# Workstreams — Coordination Ledger

**Purpose:** one section per parallel workstream so concurrent sessions (Claude Code, Claude.ai, cowork) don't collide or duplicate.
**Rule (from 2026-07-09):** every session UPDATES its own section at the end of each work block, and READS this whole file at pre-flight. One owner per line item; if it's not your section, don't edit it — flag a transfer in your own section.

---

## SIGNALS-PIPELINE — owner: Claude Code (signals/backend session)
**Last update:** 2026-07-13 night (Monday micro-brief EXECUTED; THE FLIP shipped; SHED self-heal confirmed at rollover; ohlc_bars Phase-0 evidence — Stable Engine hypothesis refuted, real driver identified)

| Item | Status | Next action | Blocked-on |
|---|---|---|---|
| L0.1a ENFORCE (Holy_Grail/CTA surface-suppression) | **LIVE + LEAKS CLOSED 7/13** (`52feaa9`+`30c3921`) — kairos badge + legacy signals trio (5 SQL stmts incl. fallbacks) now enforce; **Redis-path `/signals/queue` leak found & fixed** (new `l0_enforce_filter_rows` Python twin); ARTEMIS_LONG backfill **1,439 rows** flipped w/ provenance (307 tag-false + 1,132 no-tag; A6 invariance OK, assertion=0, pre-image `C:\temp\backfill_preimage_ARTEMIS_LONG_20260713T182250Z.jsonl`). All 7 read surfaces = 0 ARTEMIS_LONG | Re-snapshot ~7/16; RTH rider (item 8): first fresh RTH ARTEMIS_LONG carries would_suppress=true + in no feed | — |
| flow_radar db_fallback (F1/F2/F3 + 72h widen) | **DONE** — verified end-to-end 7/3 (`source:redis` + natural `db_fallback` + `flow_redis` stale) | 72→96h lookback decision (deferred; 72h holding fine) | — |
| Triton Step-0 shadow logger + grader | **LIVE** — grader sign-check PASSED 7/9 (dir-adjusted signs correct); T+1 fills daily, T+5 gates graded_at. **Post-shed recovery OBSERVED (not inferred), 2026-07-14**: 154 rows landed in `triton_flow_shadow` today, first at 13:31 UTC (7:31 AM MT — the first possible tick after RTH open) through 19:45 UTC, gap-free. Poller is RTH-gated (9:30–16:00 ET weekdays), not 24/7 — confirmed in `main.py`'s `triton_shadow_poller_loop()` — so this was the correct proof point, not the overnight window | Forward-edge study at n≥150 fully-graded (est ~2-4wk) | needs T+5 accrual |
| UW budget watchdog (in-hub circuit breaker + daily-burn snapshot) | **24/7 AS OF 7/13 (`52feaa9`)** — RTH weekday+hour gate DELETED (+ dead pytz/_dt imports); alert/escalation/shed now run every ~5-min tick on the UTC day. Post-deploy tick healthy (`uw_budget_watchdog` last_success 18:27:58 UTC, 0 failures). Shipped 7/10 (`3059573`) | **Item 7 proof pending-evening:** a success tick in `stable_job_status` AFTER 20:00 UTC Monday (post-close) is the empirical 24/7 confirmation. Burn 7/13 midday = 11,686 (well under 17K) | — |
| ohlc_bars audit + quota rebalance (governor-enforce prereq) | **FIX SHIPPED 2026-07-14 ~1:50pm MT (`b918aec`), same day as the Phase-0 audit.** Root cause (Phase-0, 7/13 night): 7/8 Stable Engine restore hypothesis REFUTED — `stable_engine/` is yfinance-only, zero UW calls, confirmed via docstrings + full-package grep. Real driver: `enrichment/universe_cache.py::refresh_universe()` — 189 active `watchlist_tickers`, refetched unconditionally every 30 min for 9h (8am–5pm ET) despite each entry already carrying its own 2h Redis TTL → ~3,402 calls/day from this ONE path, ~79% of 7/13's 4,326 `ohlc_bars` total (18,504 full-day burn, crossed 18K escalation). **Fix**: `refresh_universe()` now skips a ticker via `client.exists()` when its cache entry hasn't expired, cutting this path to ~4-5 refreshes/ticker/day instead of 18. Also fixed the stale "Try Polygon bars first" comment (code calls UW). Deploy confirmed live (`b918aec`). **Crypto resolver (`3b91328`) confirmed same-budget** via the shared `_uw_request()`→`increment_daily_counter` entry point; 0 pending CRYPTO signals as of 7/13 night, near-zero overnight risk so far — caveat stands: no per-symbol dedup once real pending crypto signals start flowing | **7/15 acceptance test (amended by Fable, 7/14 close): decoupled from Brief 3 planning.** Morning 5-min check only — crypto resolver's first clean overnight count, intraday trajectory sane, no shed fired; do NOT wait for full-day numbers before starting Brief 3 Titans pass/brief-writing (see MCP Brief 3 row). Full acceptance grade (PASS = total ~14–15K, `ohlc_bars` ~1,600–1,900 i.e. ~1.1–1.25× its 1,500 sub-quota, don't chase the residual; FAIL = trending >16K by midday or any shed → escalate, second trim candidates exist) is a **full-day** read via `uw_daily_burn`, checked after close. Only Brief 3's BUILD/DEPLOY stays gated on that PASS — resolves itself before a brief could exist to build from anyway | — |
| Write-path census (equity + shared paths only) | **RESCOPED 2026-07-13 (Fable)** — crypto write-path bypass (830/963 `signals` rows never reaching `signal_outcomes`) is CONFIRMED (per `a669429` S-1 phase-0) and now OWNED by the Stater Swap lane (`a669429`/`3b91328`), not Agora. Agora's census = equity + shared paths only; reuse S-1's findings rather than re-deriving them. Still **NOT STARTED** on the equity side | Enumerate every equity `signals` writer, confirm chokepoint (`process_signal_unified`) coverage, route/hook bypasses. Absorbs dashboard forensics #3 (`/log-signal` @ `analytics/api.py:2072`) | — |
| Discord suppression (Holy_Grail real-time alerts) | Backlog | Mini-brief — ingestion-divert vs dispatch guard; persistence untouched | — |
| **PYTHIA MP feed outage — SPY/QQQ only** | **Root cause REVISED 2026-07-15 evening (Fable, full TV log export analysis) — expiry theory retired.** Actual mechanism: **ONE watchlist alert covering ~240 symbols, only 39 calculating at any time (TradingView per-script calc-slot cap), 201 dark including SPY/QQQ.** The survivor set reshuffles on watchlist edits — this is the same mechanism behind the whole hub-side decay history (6/10 decay to 3, 6/29 decay to 23, this 7/1 decay). Not TV-side alert expiry, not account-wide (amends an earlier same-day "account-wide" characterization — not this repo's ledger, corrected here on first landing). Current alert instance born **2026-07-14 16:37 UTC**; the prior alert was deleted (history purged) — who/why still pending Nick's confirmation. **Audit note stands: Olympus committee passes 7/2–7/15 citing SPY/QQQ MP levels were reading stale July-1 structure** — treat as compromised input for that window, not re-litigated after the fact. Root cause: **CONFIRMED** (above). Restored: **PENDING**. **Trojan-Horse check (AEGIS-flavored) resolved**: all 66 payloads since 6/15 with `secret=""` are **REJECTED** — `webhooks/pythia_events.py:78-86` is fail-closed-since-inception (`hmac.compare_digest`, no observe-mode fallback, unconditional 503 if unconfigured / 401 if mismatched). Not an auth gap; those payloads never landed. Cross-checked `footprint_long`'s known-red test failure for a connection — none found, it's an unrelated classifier-threshold test (`test_feed_tier_classifier_v2.py:100`), not webhook-auth-adjacent. **Residual open item, not resolved by this check**: TV-family (`_tv_observe`, `WEBHOOK_TV_ENFORCE`) and Hermes (`_hermes_observe`, `WEBHOOK_HERMES_ENFORCE`) webhooks DO have a genuine fail-open observe-mode fallback — whether those `_ENFORCE` flags are currently set in Railway can't be confirmed from the repo (live config, not git-tracked); worth a direct Railway check if this incident report extends beyond PYTHIA. **Secrets check**: `PYTHIA_WEBHOOK_SECRET`/`TRADINGVIEW_WEBHOOK_SECRET`/`HERMES_WEBHOOK_SECRET` are three distinct, deliberately-isolated env vars by design (documented rationale in `2026-06-09-b4-chunkB-cutover-runbook.md` R-1 and `hermes.py:66-70`) — architecturally intentional, not a discovered anomaly. Can't verify the live Railway *values* actually differ from repo alone | Nick confirms root cause (who/why deleted the prior alert) + recreates/fixes coverage; restored-date lands here once live. Watchlist split-vs-trim decision: see per-name monitor row below (consumer audit now resolves this) | Nick's TV investigation |
| **PYTHIA Pine v2.4 drift + secret hygiene** | **DRIFT RECONCILED + HARDENED, 2026-07-15 evening (`15dfd42`).** Nick pasted the actual live script. Confirmed drift was real: live payload carried `secret` (see below) + `bar_time` (`str.tostring(timenow)`, wall-clock, not bar timestamp) fields the repo copy lacked. **Bigger finding than "drift": the live script hardcoded the webhook secret as a literal string directly in the payload**, not read from an input field — that's the actual mechanism behind the CSV/chat exposure, not incidental to it (a hardcoded value leaks on every share/export/screenshot of the script). Hardened in the same commit: secret now comes from `webhookSecret = input.string("", ...)`, matching every other TV-family script's established pattern (`artemis_v3.pine` etc.) — filled in per-alert in TradingView's UI, never in the script body. Verified zero occurrences of the actual secret value anywhere in the file/commit before pushing. **New exposure surface, not yet in the rotation's stated rationale**: the live secret value was also pasted into this chat session to produce the diff — that's a second exposure channel (chat transcript) alongside the CSV one, on top of the already-hardcoded-in-script exposure this fix just closed. Worth Nick weighing whether that moves the rotation earlier than "post-stability." **Secret rotation still QUEUED** (Railway env + this field + re-arm all 4 Pythia alerts, one coordinated pass — a partial rotation just creates a 4th outage flavor); old value still live and still compromised until it lands. **SPY/QQQ/IWM dedicated alerts ARMED 2026-07-15 evening** — round-trip verification (first fire landing in `pythia_events`) still pending | Confirm SPY/QQQ/IWM's first alert fire lands correctly. Nick's call on rotation timing given the added chat-transcript exposure | First alert fire; Nick's rotation-timing call |
| **15:00 UTC daily webhook-timeout — root cause FOUND, event-loop-blocking bug** | **New finding 2026-07-15 (Fable's check #2).** TICK webhook timed out at exactly 15:00:00 UTC on 7/10, 7/13, 7/14. Root cause: `bias_scheduler.py:2611-2617`'s `bias_refresh_1100` cron job (`CronTrigger(hour=11, minute=0, day_of_week='mon-fri')`, scheduler timezone = ET) fires at 11:00 AM ET = **15:00 UTC exactly in EDT (summer)** — matches all 3 incident dates precisely. Inside it, `refresh_weekly_bias()` calls `scanner.get_technical_analysis(ticker, interval="1W")` (`bias_scheduler.py:1118` etc.) for ~11 tickers (SPY/QQQ/IWM/SMH/UUP/XLK/XLU/HYG/TLT/RSP) — this is a **plain synchronous `def`** (`hybrid_scanner.py:265`) making blocking `requests`-based HTTP calls via `tradingview-ta`'s `TA_Handler`, with **no `asyncio.to_thread`, no executor** anywhere in either file. `interval="1W"` is never cached (only `"1d"` is), so this fires fresh every scheduled run. Since this job runs on the SAME event loop that serves `/webhook/tick` (`main.py:106-107` startup, same loop as the webhook router), ~11 sequential blocking network calls stall the entire event loop for the duration — textbook mechanism, and it explains the exact-second timing precisely, not coincidentally. **Recommended fix (not applied, read-only per this check): wrap `scanner.get_technical_analysis(...)` calls in `refresh_daily_bias()`/`refresh_weekly_bias()` in `asyncio.to_thread`** so the webhook router keeps serving concurrently | Small, well-scoped fix once picked up — same "wire into existing async patterns" class of work as other recent hygiene items. Not sequenced yet | — |
| **7/14 blackout-override cost — 3 signals lost, evidenced** | **Confirmed via DB query 2026-07-15.** 3 Artemis signals landed 19:52:44 / 19:54:51 / 19:55:05 UTC (13:52:44 / 13:54:51 / 13:55:05 MT) on 7/14 — `ARTEMIS_XLY_20260714_195244_319711` (DISMISSED), `ARTEMIS_MAR_20260714_195451_042018` (DISMISSED), `ARTEMIS_LLY_20260714_195505_473913` (EXPIRED) — landing right inside/after this session's blackout-window pushes that same afternoon (`b918aec` 13:48:32 MT, `03f9b30` 13:49:28 MT, `49d7d9b` 13:53:06 MT), which Nick separately ruled an explicit override, no rule change. **New evidence for the record, not a re-litigation of that ruling**: TradingView does not retry a failed webhook POST, so a mid-flight Railway redeploy's ~60-170s outage window doesn't just delay a signal, it **destroys** it — a materially sharper cost than "the hub was briefly unreachable." No single canonical file holds "the blackout rule's rationale" (it's referenced across `docs/handoff-monday-2026-07-13.md`'s standing-rules line and this session's own memory, not centralized) — flagging that gap rather than guessing where to append. This session's memory (`feedback_rth_blackout_contingent.md`) updated with this concrete cost so future sessions weigh it, not just "is Nick watching" | If Nick wants this formalized, `PROJECT_RULES.md` doesn't currently have a Deployment Rules entry for the RTH blackout at all — worth adding one line there as the canonical home | Nick's call on formalizing |
| **PYTHIA per-name staleness monitor (systemic fix) — reuses an already-written, never-built brief** | **This is the THIRD time this exact bug class has caused an undetected outage** (B4 closure 6/10: decay to 3 tickers; 6/29 review: decay to 23, SPY dark 12 days; now 7/15: SPY+QQQ dark 14 days) — same root cause each time: `config/l1_gate.py::_maybe_mp_feed_down_alarm` checks **GLOBAL** `MAX(timestamp) FROM pythia_events` (confirmed still true in code, 2026-07-15), so any surviving liquid ticker (AAPL/NVDA/etc.) keeps the global timestamp fresh and masks individual dead names — exactly the blind spot `docs/codex-briefs/2026-06-29-pythia-mp-feed-reliability-titans-brief.md` diagnosed and speced a fix for. **That brief's Part 1 (per-name freshness monitor, mirrors the existing Chunk-3 `flow_deadfeed_watchdog_loop` pattern) was never built** — status still reads "FOR TITANS REVIEW (not yet built)" 2+ weeks later. Recommend **reusing/updating that brief rather than writing a new one** — its design already matches Fable's sharpened ask (alarm when a roster ticker, SPY/QQQ minimum, has no `pythia_events` row for >1 full session); its Part 2 (feed-shed root-cause) is now largely redundant with Nick's live TV investigation above. Needs a fresh recon-anchor re-check (`2617c31` is 2+ weeks stale) before building. **Assessed: not trivial** — real watchdog-loop + per-ticker-latch work, same size class as the Chunk-3 brief it mirrors — so per Fable's own conditional, this slots after Brief 3's build, not ahead of it. Flagging the 3rd-recurrence pattern itself as the actual prioritization signal: each cycle costs ~2 weeks of silently-corrupted committee inputs before detection. **STOPGAP per-ticker roster alarm still pending Nick's GO** (Fable, 7/15 evening) — the TV export is the closing argument (coverage is provably arbitrary, hub must own its own roster expectations), but the 6/29 brief's Part 1 remains the durable fix either way. **Consumer audit DONE 2026-07-15 (Fable's check #4) — resolves the split-vs-trim question toward SPLIT, not trim.** Two consumer classes found: (1) a **narrow, named** set — L1a gate's `LIQUID_UNIVERSE` (20 tickers) + WH-REVERSAL's `WH_TICKER_UNIVERSE` (25 tickers), 14 overlap = 31 distinct names, both already-narrow-by-construction and unaffected by the other ~209 watchlist symbols; (2) a **broad, live, unconditional** consumer — `signals/pipeline.py:556-583`'s P4B coverage check, which runs on essentially every scored signal (not shadow-gated like L1a) against a dynamically-built ~200-ticker scan universe (`scanners/universe.py`), and directly caps `feed_tier_ceiling` to `"watchlist"` on a miss. **Historically measured, not theoretical**: `docs/phase0-l1-findings.md` — PYTHIA coverage ~25-27% across 1,070 signals/14d; the 6/29 L1a week-1 review found 11 of 20 liquid names had ZERO `pythia_events` that week while ~40 non-liquid names were calculating instead — feed misalignment, not just gaps. That same review explicitly rejected narrowing to survivors as a fix ("no cost case for it — would permanently amputate liquid names to accommodate a feed that's simply broken and cheaply restorable"). Given P4B's broad reach (~92% of scored signals) and this precedent, **splitting the alert to preserve broad coverage is better-evidenced than trimming to the 31-name union** — trimming would still leave the large majority of P4B's real-world impact unaddressed | Slot after Brief 3 build. When picked up: refresh the 6/29 brief's recon anchor, confirm Part 2 is supersedable by Nick's TV finding, build Part 1 only unless the TV root cause reopens Part 2's scope. Split-vs-trim resolved toward split — factor into the watchlist-restructure decision alongside the fix | Brief 3 build (per Fable's sequencing); Nick's GO on the stopgap |
| **P0: MCP import-safety lint never ran in CI (glob bug)** | **Found 2026-07-15, AEGIS pass on Brief 3.** `.github/workflows/mcp-lint.yml` triggers on `backend/mcp/**`; the real module is `backend/hub_mcp/**` (renamed same commit the workflow was added, `52e1038`) — the glob has never matched, on any of the 13 existing tools, ever. Layer 2 of the 3-layer read-only enforcement (whitelist / import-lint / AEGIS-review) has effectively been running as layer-3-only since inception. **Ran `scripts/lint_mcp_imports.py` LOCALLY against the current 13 tools before touching the CI fix (per Fable's sequencing ask) — result: PASS, 0 violations** (verified 2026-07-15). Also independently confirmed `backend/hub_mcp/` currently has zero imports from `api.stable`/`api.board_state` (the specific gap AEGIS flagged in `FORBIDDEN_MODULES`) — the gap exists in the denylist but isn't currently exploited by any shipped code. **No surprise red build; clean baseline confirmed deliberately, not assumed** | Fix lands as Brief 3 Task 0 (glob fix) + Task 10 (denylist entries), in that order — CI fix gated on tonight's ohlc_bars burn PASS along with the rest of Brief 3's build/deploy | ohlc_bars burn PASS |
| **LAZR/Robotics upstream score-computation fix (orphaned P3 item)** | **STILL OPEN — not touched by Brief 3.** Robotics theme score pinned at 100.0 by a delisted/recycled ticker (LAZR); Brief 3's `hub_get_stable_themes` ships a **tool-output guard only** (hardcoded `data_quality_warnings` flag) so the MCP surface doesn't silently hand a corrupted number to the committee — it does NOT fix the underlying `stable_theme_scores` computation. Root-cause fix (3 layers: evict LAZR + dead-ticker hygiene, sanity-bounded calendar-aware `pct_change`, quarantine-on-resume) remains scoped-but-unbuilt since 7/10 | Separate brief, not yet written. Robotics stays KNOWN-BAD (dashboard AND now MCP, guard-flagged) until it ships | — |
| **Legacy `/app/legacy` removal — HOLD (flag for DASHBOARD-V2)** | **Nick decision 2026-07-14 1:45pm MT: legacy dashboard removal is now HELD until S-6 (Stater Swap v2 UI port into `/app/v2`'s design system, HELIOS mockup-gate applies) ships.** This supersedes DASHBOARD-V2's "Flip gates" row (line 36), which currently targets legacy removal ~2026-07-20 — that date no longer holds. Not my section to edit directly (ledger ownership rule) — flagging here per the rule's own "flag a transfer in your own section" convention so DASHBOARD-V2 picks it up and updates their own row | **DASHBOARD-V2: update "Flip gates" row's legacy-removal target to reflect the hold, do not remove `/app/legacy` on the old ~7/20 date** | S-6 ship date (not yet scheduled — S-1 is still in progress as of this flag) |

**Notes / evidence (7/9):** enforce=true; `TRITON_SHADOW_ENABLED` UNSET (=enabled, no shed fired); UW total 7/9 = **16,626** (< 17K trigger; 3 benign 429s; no 20K breach). 7/6-7/8 daily totals UNRECOVERABLE (Redis 48h TTL expired) — Triton row/ticker counts those days were comparable to 7/9. `ohlc_bars` 7/9 = 3,602 (still ~2.4× its 1,500 quota).

**Watchdog build (`3059573`, shipped 7/10):** in-hub runtime circuit breaker (`jobs/uw_budget_watchdog.py`) — ~5-min tick reads `get_daily_count`; ≥17K sets Redis `quota_shed:triton` (TTL→UTC rollover), Triton poller skips at top of cycle, ONE Discord CB-channel alert + `stable_job_status` record; ≥18K human-call escalation naming next-tier candidates (no further auto-shed). Daily-burn snapshot → `uw_daily_burn` (mig 022) kills the 48h TTL blindness. **Pre-reg amendment:** runtime flag REPLACES `TRITON_SHADOW_ENABLED=false` env shed (env forced mid-session redeploy = RTH-blackout violation; env now manual fallback only). **HANDOFF to DASHBOARD-V2:** the "river action item" is a UI surface — backend has no river table; the alert lands on Discord CB + `stable_job_status`. If a dashboard action-items river exists, wire it to read `stable_job_status` job=`uw_budget_shed` (or the `quota_shed:triton` flag).

**Watchdog DAY-1 OUTCOME (7/10 — SUPERSEDES the earlier "clean no-fire = PASS" line):** watchdog PASS *within its designed window* (RTH), but the **window assumption was FALSIFIED by a post-close crossing**: 7/10 final **17,394** (> the 17K trigger) accumulated *after* the 16:00 ET gate closed, so the RTH-gated watchdog never saw it (last tick 19:58 UTC @ ~15,870). The **24/7 `uw_daily_burn` snapshot caught it** — my live 20:09 UTC check read 16,262/under, a false all-clear. Impact today LOW (shedding idle Triton post-close is moot; 17,394 < 18K escalation), but a genuine gap. Fix (Fable, HELD → Monday item 4 / ATLAS): delete the RTH gate; alert/escalation 24/7 (a wider fixed window repeats the assumption class that just failed — watch the UTC day); shed ungated. **TREND FLAG:** 7/9 16,626 → 7/10 **17,394**, two consecutive days climbing toward the 20K cap; **18K-escalation margin halved (1,374 → 606)**; 7/10 within ~13% of the cap. Day-over-day attribution: `ohlc_bars` +479 / `option_contracts` +347 = the whole climb (feeds the ohlc_bars audit).

**MONDAY 7/13 MICRO-BRIEF EXECUTED (Nick cleared the blackout; burn safe at 11,686):** Commits `2513620` (brief) → `52feaa9` (items 2+3+5a L0 read surfaces + 4+5b watchdog 24/7) → `3578151` (backfill runbook) → `30c3921` (/queue Redis leak fix). Deploy `30c3921` healthy. **Items 1–5 all DONE + accepted.** Test suite: only the 3 ledgered known failures (footprint_long, session_sweep, pullback_entry) — zero new. **DEVIATION from brief (flagged + GO'd by Nick):** brief Item 3 assumed `/signals/queue` was backed by the patched SQL `get_signal_queue`; it actually reads the Redis cache first (`get_active_signals`), which the SQL WHERE never touches → 10 suppressed rows leaked. Fixed with a Python-side enforce twin (`l0_enforce_filter_rows`), applied in [positions.py](../backend/api/positions.py) queue route. **Backfill script `scripts/backfill_suppression.py` is now a reusable eviction runbook** (A1 pre-image / A5 --i-have-go gate / A6 invariance / A7 param-binding). **PENDING-EVENING:** item 7 (post-20:00-UTC watchdog tick) + item 8 (first fresh RTH ARTEMIS_LONG suppressed in all feeds).

---

## DASHBOARD-V2 — owner: Claude Code (dashboard session)
**Last update:** 2026-07-13 17:40 MT (c4/THE FLIP shipped by SIGNALS-PIPELINE CC — DASHBOARD session handoff line was stale, this session absorbed the lane per Nick's kickoff P0)

| Item | Status | Next action | Blocked-on |
|---|---|---|---|
| Flip gates | **c4 DONE — THE FLIP shipped 2026-07-13 17:40 MT (`e9a4840`).** `/app` now serves `v2.html` (was `index.html`); `/app/legacy` serves the old dashboard via the pre-existing `/app/{mode}` catch-all (no backend change needed there); `/app/v2` kept as a harmless post-flip alias. Fixed v2.html's topbar link (was self-referential `/app`→"Legacy", now `/app/legacy`). Deploy verified: hub boot-time/SHA match confirmed by coordination lane, `redis`/`postgres` unaffected, ~50s cold start. Route check: all 3 paths confirmed serving correct content. **ACK — SHA `e9a4840`, pushed 2026-07-13 17:40 MT, executed by this session (SIGNALS-PIPELINE CC) per Nick's explicit "FLIP: GO. Execute now" 2026-07-13 evening.** Process note acknowledged: production GOs should get this explicit ACK+SHA line going forward — this flip's completion report initially only landed in chat, not the ledger; back-filling now | **Nick pixel-check: PASS (2026-07-14 close)** — full RTH session traded on the flipped `/app` today was the evidence; live page matches approved v2 mockup v3 + 6c capture. **Flip fully accepted, all gates cleared.** **Legacy removal HELD 2026-07-14 1:45pm MT (Nick decision, flagged by SIGNALS-PIPELINE row 20) — no longer targeting ~7/20.** `/app/legacy` stays live until S-6 (Stater Swap v2 UI port into `/app/v2`'s design system, HELIOS mockup-gate applies) ships; S-6 isn't scheduled yet (S-1 still in progress) | S-6 ship date |
| MCP mount outage | **RESOLVED 2026-07-11** — fastmcp==3.4.4 pinned (`aaea01c`); `/mcp/v1/health` 200, discovery self-check green. Do NOT toggle the connector (manifest unchanged) | — | — |
| Kairos fidelity 6a–c | **SHIPPED 2026-07-11 (`dd28bd6`)** — roster gate (ACHILLES = 6th setup), grade v1 (validated-cell A only, no legacy-score A's), non-roster→river +N counter; joins flip gates | Fable live-verifies | — |
| Sector RS-10d contract fix | **SHIPPED 2026-07-11 (this brief)** — writer stores rs_10d/rank_10d, reader honest-null + degraded + real staleness; T1–T4 green, local proof passed, prod Redis re-primed with new schema | Fable verifies live via MCP `hub_get_sector_strength` (do not self-grade) | — |
| Gate-1 SPY test-row neutralization | **DONE 2026-07-13 12:55 MT** (well before the 19:30 MT feedback-loop deadline) — deleted `unified_positions.id=344` (`POS_SPY_20260711_164210`), `trades.id=544`, `closed_positions.id=142` (transaction, per-statement RETURNING row-count guard, all 3 confirmed exactly 1 row before commit). Independently verified: 0 RT-TEST rows remain anywhere; `/api/analytics/trade-stats` + `/api/analytics/trades` + cockpit/laboratory dashboards no longer carry the phantom +$100 WIN / +$654.95 stale-unrealized artifact. Pre-existing gap flagged (not fixed, out of scope): no `is_test` column/convention exists anywhere — 2 older unremediated test rows still sit in `trades` (`id=171` "DOUBLE_TAP_TEST", `id=126` TSLA "test-deploy-check") with identical exposure through the same unfiltered endpoints | Consider an `is_test` boolean + default-exclude filter in `get_trade_rows()` so this class of leak can't recur silently. **is_test mini-brief now has a 3rd named candidate row (Fable, 2026-07-13 night): `signals.id=14893` (`S1_PHASE2_SHADOW_TEST_BTC_20260713`) — intentionally retained as evidence, do NOT delete/neutralize; the brief needs a way to mark "keep but exclude from live surfaces" distinct from the 2 `trades` rows above, which are pure cruft** | — |
| `closed_at` defect — landing check | **CLOSED 2026-07-13 (D3) — misdiagnosis, on evidence.** No commit touches `closed_at`; `trades.closed_at`/`closed_positions.closed_at` correctly bind to `now` in `close_position()` (code unchanged since `f435d591`, 2026-04-29); `unified_positions` has no `closed_at` column by design (uses `exit_date`). Original "null on both rows" observation likely came from the close-endpoint JSON response mirroring `unified_positions` (no `closed_at` key there by design). | **Passive-verify only** on the next natural (non-test) close — confirm `trades.closed_at`/`closed_positions.closed_at` populate. No forced test close (no `is_test` convention exists yet — see Gate-1 row). Reopen with a repro if a real null shows up | — |
| Badge redo | **CLOSED 2026-07-13 (D4) — shipped under another name.** Ledger line predates the 7/11 Kairos fix (present unchanged since the file's original seed). The visible-badge substance it pointed at (roster gate, grade v1, non-roster→river counter) shipped as **Kairos fidelity 6a–c** (`dd28bd6`, 2026-07-11, row above). Note: this does NOT cover chase item 1 in `docs/handoff-monday-2026-07-13.md` ("Badge acceptance redo — committed capture script, authenticated, production URL — still owed") — that's an *evidence-capture* task, distinct from the code fix, and remains open there if still wanted | If Nick wants the capture-script evidence artifact, it's a separate P4-adjacent ask — one sentence defines scope | — |
| Forensics 1–5 | In progress | **#3 (`/log-signal` caller inventory + routing) TRANSFERRED to SIGNALS-PIPELINE write-path census.** Dashboard keeps read-side only | — |

---

## PARALLEL CHAT — owner: Claude.ai chat session
**Last update:** 2026-07-13 (stale MCP Brief 3 line fixed by consolidation-kickoff hygiene pass, cross-lane per Nick's explicit D2–D4 authorization — not otherwise this session's lane)

| Item | Status | Next action | Blocked-on |
|---|---|---|---|
| Mobile concepts | (owner to fill) | — | — |
| MCP Brief 3 | **BRIEF WRITTEN 2026-07-15 morning** — `docs/codex-briefs/hub-get-stable-tools-v1-2026-07-15.md`, staged untracked (not yet committed/pushed — held for the 07:30–14:00 MT window, no override this round; push at 14:00 MT). Titans: ATLAS + AEGIS ran single-pass (not full double-pass) parallel reviews; ATHENA synthesis done directly by the orchestrating session, not a separate blind pass. Scope Nick-approved same morning. **6 tools, not 4** — theme_members + board_state added as their own registrations, ATLAS's proposal/ATHENA-endorsed/Nick-approved, not a builder call. **LAZR guard is tool-output only**, upstream fix stays a separate open P3 item (see new row above). **UW/cost Done-criterion added**: measured per-day yfinance-call volume for the theme_members rider, verified post-deploy (these are yfinance calls, untracked by `uw_daily_burn`). AEGIS's pass also surfaced the CI-lint bug, now its own P0 ledger line (see above) — lint run locally first, clean | Push brief at 14:00 MT → CC builds on tonight's ohlc_bars burn PASS → toggle Pandora connector after ship (Nick's action). Sequenced P2 in the consolidation kickoff | ohlc_bars burn PASS (build/deploy step only) |

---

## COWORK — owner: cowork session
**Last update:** 2026-07-09 (seeded)

| Item | Status | Next action | Blocked-on |
|---|---|---|---|
| Morning Stable digest | Recurring | — | — |

---

## Cross-stream dedupe log
- **2026-07-09:** Write-path census is SIGNALS-PIPELINE's alone. Dashboard forensics #3 (`/log-signal` @ `analytics/api.py:2072` — caller inventory + routing through `process_signal_unified`) merges into it. Dashboard CC keeps read-side only. Census = Phase-0 findings first, no code before the report.
- **2026-07-11:** Sector RS-10d contract fix (`sector_momentum.py` writer + `sector_strength.py` reader) owned by DASHBOARD-V2 CC — ATLAS-approved mini-brief, one Saturday push. `scanners/sector_rs.py` (Achilles' OWN `sector_rs:{ETF}` cache) is a separate, healthy pipeline — untouched; dual-pipeline consolidation is a build-backlog note only, not claimed by any stream yet.
- **2026-07-14 BLACKOUT INCIDENT:** 3 pushes landed inside the 07:30–14:00 MT blackout window, 2 lanes: `b918aec` (13:48:32 MT, SIGNALS-PIPELINE — ohlc_bars/universe_cache fix), `03f9b30` (13:49:28 MT, Stater Swap/S1 lane — legacy-removal ledger flag), `49d7d9b` (13:53:06 MT, SIGNALS-PIPELINE — ledger update). A 4th same-day SIGNALS-adjacent push, `90f9d10` (14:04:04 MT, Stater Swap S1 phase3), landed clean outside the window. **SIGNALS-PIPELINE self-assessment: DRIFT, not a confirmed override.** Nick's 13:45 MT "run what you need to" did not reference the blackout rule or state an override; this session proceeded on a memory note (blackout is contingent on Nick actively being in the hub, not a hard clock rule) without surfacing that read or confirming it with Nick before pushing. The other lane's reasoning for `03f9b30` is theirs to account for, not assessed here. **RULED by Nick, 2026-07-14: EXPLICIT OVERRIDE** — "run what you need to" was meant to cover pushing through blackout too. No `PROJECT_RULES.md` change; incident closed.
