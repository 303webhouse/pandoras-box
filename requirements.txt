# Pandora's Box - Backend Dependencies

# FastAPI and ASGI server
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# WebSocket support
websockets>=12.0

# Database drivers
redis>=5.0.1
asyncpg>=0.29.0
psycopg2-binary>=2.9.9  # sync driver for the Stable Engine writer (stable_engine/db.py, runs in worker threads)

# Data validation
pydantic>=2.5.3

# Async support
aiofiles>=23.2.1

# Environment variables
python-dotenv>=1.0.0

# Date/time utilities
python-dateutil>=2.8.2
pytz>=2024.1

# HTTP requests
httpx>=0.26.0

# Hunter Scanner - Market Data & Analysis
yfinance>=0.2.36
pandas>=2.0.0
pandas_ta>=0.3.14b

# Hybrid Scanner - TradingView Technical Analysis
tradingview-ta>=3.3.0

# Discord Bridge - Unusual Whales Integration
discord.py>=2.3.2
aiohttp>=3.9.0

# Scheduler for automated bias refresh
apscheduler>=3.10.0

# FRED API - Federal Reserve Economic Data
fredapi>=0.5.1

# Google Gemini API - AI-powered data extraction
google-generativeai>=0.8.0

# Anthropic Claude Vision API
anthropic>=0.18.0

# Model Context Protocol — Streamable HTTP server for Olympus hub access
# Pinned (not >=): 2026-07-09 the connector went down (421 Misdirected
# Request on every FastMCP-served path) and cleared on redeploy without any
# app-code change — the unbounded >=3.3.1 pin meant Railway could silently
# resolve a different fastmcp release on each rebuild. 3.4.4 is the version
# confirmed working via /mcp/v1/health's package_versions field. Bump this
# deliberately, not by drift.
fastmcp==3.4.4
