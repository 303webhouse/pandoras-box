# Pivot agent environment variables
# Copy to /opt/pivot/.env and fill in real values.

# ── Pandora's Box API ─────────────────────────────────────────────────────────
PANDORA_API_URL=https://pandoras-box-production.up.railway.app/api
PIVOT_API_KEY=CHANGE_ME

# ── Data sources ──────────────────────────────────────────────────────────────
FRED_API_KEY=CHANGE_ME

# ── LLM (for morning/EOD briefs) ─────────────────────────────────────────────
# Supports openrouter (default), anthropic, openai
LLM_PROVIDER=openrouter
LLM_API_KEY=CHANGE_ME
LLM_MODEL=anthropic/claude-haiku-4-5

# ── Discord webhooks (pivot-collector notifications) ──────────────────────────
DISCORD_WEBHOOK_CRITICAL=CHANGE_ME
DISCORD_WEBHOOK_SIGNALS=CHANGE_ME
DISCORD_WEBHOOK_BRIEFS=CHANGE_ME
DISCORD_WEBHOOK_CALENDAR=CHANGE_ME
DISCORD_WEBHOOK_SYSTEM=CHANGE_ME

# ── Discord bot (pivot-bot) ───────────────────────────────────────────────────
DISCORD_BOT_TOKEN=CHANGE_ME

# Unusual Whales channel/user IDs (right-click → Copy ID in Discord dev mode)
UW_FLOW_CHANNEL_ID=CHANGE_ME
UW_TICKER_CHANNEL_ID=CHANGE_ME
UW_BOT_USER_ID=CHANGE_ME

# ── Interactive Chat ──────────────────────────────────────────────────────────
# Setup instructions:
#   1. Create a #pivot-chat channel in your Discord server
#   2. Right-click the channel → Copy ID → set PIVOT_CHAT_CHANNEL_ID
#   3. Right-click your username → Copy ID → set PIVOT_OWNER_ID
#   4. Ensure the Pivot bot has: Send Messages, Read Messages, Embed Links
PIVOT_CHAT_CHANNEL_ID=CHANGE_ME
PIVOT_OWNER_ID=CHANGE_ME

# Optional: Brave Search API key (used by the bot for news lookups)
BRAVE_API_KEY=

# ── Flow filter settings ──────────────────────────────────────────────────────
UW_MIN_DTE=7
UW_MAX_DTE=180
UW_MIN_PREMIUM=50000
DISCORD_MIN_SCORE_FOR_ALERT=80

# ── General settings ─────────────────────────────────────────────────────────
DISCORD_MIN_PRIORITY=MEDIUM
HEALTH_OFFLINE_THRESHOLD_MINUTES=30
TIMEZONE=America/New_York
